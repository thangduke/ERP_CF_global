<odoo>
    <data>
        <record id="view_asset_type_search" model="ir.ui.view">
            <field name="name">asset.type.search</field>
            <field name="model">asset.type</field>
            <field name="arch" type="xml">
                <search string="Asset Type">
                    <field name="name"/>
                    <field name="category_id"/>
                    <filter string="Archived" name="inactive" domain="[('active', '=', False)]"/>
                    <group expand="0" string="Group By">
                        <filter string="Category" name="category" context="{'group_by':'category_id'}"/>
                    </group>
                    <searchpanel>
                        <field name="category_id" string="Danh mục tài sản" icon="fa-folder" enable_counters="1"
                            options="{'always_loaded': True}"/>
                    </searchpanel>                   
                </search>
            </field>
        </record>

        <record id="view_asset_type_form" model="ir.ui.view">
            <field name="name">asset.type.form</field>
            <field name="model">asset.type</field>
            <field name="arch" type="xml">
                <form string="Asset Type">
                    <sheet>
                        <div class="oe_button_box" name="button_box">
                            <button name="toggle_active" type="object"
                                    class="oe_stat_button" icon="fa-archive">
                                <field name="active" widget="boolean_button"
                                    options='{"terminology": "archive"}'/>
                            </button>
                        </div>
                        <group>
                            <group>
                                <field name="name" style="border: 1px solid #ced4da;"/>
                                <field name="category_id" style="border: 1px solid #ced4da;"/>
                            </group>
                            <group>
                                <field name="employee_id" widget="many2one_avatar_user" style="border: 1px solid #ced4da;"/>
                            </group>
                        </group>
                        <notebook>
                            <page string="Assets">
                                <field name="asset_line_ids"/>
                            </page>
                            <page string="Description">
                                <field name="description" style="border: 1px solid #ced4da;"/>
                            </page>
                        </notebook>
                    </sheet>
                    <div class="oe_chatter">
                        <field name="message_follower_ids"/>
                        <field name="activity_ids"/>
                        <field name="message_ids"/>
                    </div>
                </form>
            </field>
        </record>

        <record id="view_asset_type_tree" model="ir.ui.view">
            <field name="name">asset.type.tree</field>
            <field name="model">asset.type</field>
            <field name="arch" type="xml">
                <tree string="Asset Types" class="o_general_list_view_product">
                    <field name="category_id"  string="DANH MỤC"/>
                    <field name="name"   string="CHỦNG LOẠI"/>
                    <field name="active" string="TRẠNG THÁI" widget="boolean_toggle"/>
                    <field name="description" string="MÔ TẢ"/>
                    <field name="employee_id" widget="many2one_avatar_user" string="NGƯỜI TẠO" optional="hide"/>
                    <field name="create_date" string="NGÀY TẠO" optional="hide"/>
                </tree>
            </field>
        </record>

        <record id="action_asset_type" model="ir.actions.act_window">
            <field name="name">Asset Types</field>
            <field name="res_model">asset.type</field>
            <field name="view_mode">tree,form</field>
            <field name="search_view_id" ref="view_asset_type_search"/>
        </record>
    </data>
</odoo>